# Gemini / Antigravity Instructions — Cytognosis Project

You are assisting development on **cyto-assist**, a Cytognosis healthcare AI project.

## Key Files
- `pyproject.toml` — project configuration and dependencies
- `src/cyto_assist/` — source code
- `tests/` — test suite
- `.agents/config.yaml` — agent configuration
- `scripts/setup_env.py` — environment management

## Conventions
- Use `uv` for package management
- Use `nox` for task automation
- Follow conventional commits
- Write pytest tests for all new code
- Use Ruff for formatting and linting

## Skills
Refer to `.agents/skills/` for detailed domain-specific instructions.

## Workflows
Refer to `.agents/workflows/` for multi-step procedures.
